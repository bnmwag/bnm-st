---
import { Container, Grid } from "@/components/core";

const links = [
    { label: "About", href: "/about" },
    { label: "Contact", href: "/contact" },
];
---

<nav data-site-nav aria-label='Site' class='fixed inset-x-0 top-grid-margin z-50 mix-blend-difference text-white'>
    <Container>
        <Grid>
            <div class='col-span-4 max-lg:col-span-7 flex flex-col items-start gap-1'>
                <a href='/' class='text-label uppercase transition-opacity hover:opacity-50 -my-2 py-2 -mx-1 px-1'>Benjamin Wagner</a>
                <p class='text-label uppercase opacity-50'>developer + designer</p>
            </div>
            <div class='col-span-4 max-lg:hidden'></div>
            <div class='col-span-4 max-lg:col-span-5 flex flex-col items-end gap-1 text-right'>
                {
                    links.map((link) => (
                        <a href={link.href} class='text-label uppercase transition-opacity hover:opacity-50 -my-2 py-2 -mx-1 px-1'>
                            {link.label}
                        </a>
                    ))
                }
            </div>
        </Grid>
    </Container>
</nav>
